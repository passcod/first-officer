name: Typos Check
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  run:
    name: Spellcheck
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v6
      - name: Check spelling
        uses: crate-ci/typos@v1.44.0
        with:
          config: .typos.toml
